const settings = {
    httpPort: 30001,
    httpsPort: 30000,
    mediaPath: "\\\\nas.home\\media\\video\\web",
    keyPath: "./certs/key.pem",
    certPath: "./certs/cert.pem"
};

module.exports = settings;